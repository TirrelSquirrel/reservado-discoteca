import React from 'react';
import { Outlet } from 'react-router-dom';

const EditReservadosContainer = () => {
    return (
        <>
            <Outlet />
        </>
    );
}

export default EditReservadosContainer;
